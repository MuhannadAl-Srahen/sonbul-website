---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Contact from '../../components/pages/Contact';
import en from '../../i18n/locales/en.json';
import ar from '../../i18n/locales/ar.json';
import type { Lang } from '../../i18n';

export function getStaticPaths() {
  return [{ params: { lang: undefined } }, { params: { lang: 'ar' } }];
}

const lang = ((Astro.params.lang as string | undefined) ?? 'en') as Lang;
const t = lang === 'ar' ? ar : en;
---

<BaseLayout title={t.nav.contact} description={t.contact.hero.subtitle} path={Astro.url.pathname} lang={lang}>
  <Contact lang={lang} client:load />
</BaseLayout>
