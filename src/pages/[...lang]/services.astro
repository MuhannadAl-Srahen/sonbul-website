---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Services from '../../components/pages/Services';
import en from '../../i18n/locales/en.json';
import ar from '../../i18n/locales/ar.json';
import type { Lang } from '../../i18n';

export function getStaticPaths() {
  return [{ params: { lang: undefined } }, { params: { lang: 'ar' } }];
}

const lang = ((Astro.params.lang as string | undefined) ?? 'en') as Lang;
const t = lang === 'ar' ? ar : en;
---

<BaseLayout title={t.nav.services} description={t.services.hero.subtitle} path={Astro.url.pathname} lang={lang}>
  <Services lang={lang} client:load />
</BaseLayout>
